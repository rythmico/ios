import SwiftUI

struct AcceptedStatusPill: View {
    var body: some View {
        Pill(
            title: "Accepted",
            titleColor: .rythmicoDarkGreen,
            backgroundColor: .rythmicoLightGreen
        )
    }
}
