import SwiftUI

struct SchedulingView: View {
    var body: some View {
        TitleSubtitleContentView(title: "Lesson Schedule", subtitle: []) {
            VStack {
                Spacer()
                Text("Coming next!")
                Spacer()
            }
        }
    }
}
