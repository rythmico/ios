import FoundationEncore

extension LessonPlan.BookingInfo {
    static let jesseStub = Self(date: .stub, tutor: .jesseStub)
    static let davidStub = Self(date: .stub, tutor: .davidStub)
    static let charlotteStub = Self(date: .stub, tutor: .charlotteStub)
}
