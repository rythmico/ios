#!/bin/bash

if ! which swiftgen > /dev/null; then
    HOMEBREW_NO_AUTO_UPDATE=1 \
    brew install swiftgen
fi

swiftgen run xcassets \
    "$TARGET_NAME/Supporting Files/Assets.xcassets" \
    --templateName swift4 \
    --output "$TARGET_NAME/Supporting Files/Assets.generated.swift"
